extends layout

block content
  h1= article.title
  h5 Written by #{article.author}
  p= article.body
  hr
  a.btn.btn-default(href='/article/update/'+article._id) Update
  br
  a.btn.btn-danger.delete-article(href='#',data-id=article._id) Delete
